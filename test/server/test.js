// Dependencies
const expect = require('chai').expect;
// const chaiHttp = require('chai-http');

describe(('TEST'), () => {
  it('hey', done => {
    expect('hey').to.be.equal('hey');
    done();
  });
});
